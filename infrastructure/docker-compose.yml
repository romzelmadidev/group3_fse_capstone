networks:
  banking-net:
    name: banking-net
    driver: bridge

services:
  # ============================================================================
  # 1. Master Relational Database (Oracle Database Express Edition 21c)
  # Container Name: oracle-xe-master
  # Houses operational state: users, accounts, balances, transactions, outbox
  # ============================================================================
  oracle-xe-master:
    container_name: oracle-xe-master
    image: gvenzl/oracle-xe:latest
    restart: unless-stopped
    ports:
      - "1521:1521"
    environment:
      - ORACLE_PASSWORD=Password123#
      - APP_USER=fse_user
      - APP_USER_PASSWORD=fse_password
    volumes:
      - ./oracle/init.sql:/container-entrypoint-initdb.d/init.sql:ro
      - oracle_data:/opt/oracle/oradata
    networks:
      - banking-net
    healthcheck:
      test: ["CMD-SHELL", "/opt/oracle/checkDBStatus.sh || exit 0"]
      interval: 10s
      timeout: 5s
      retries: 20
      start_period: 30s

  # ============================================================================
  # 2. Dedicated Immutable Audit Vault (PostgreSQL 16)
  # Container Name: postgres-audit-vault
  # Houses append-only mutation journal protected by compliance triggers
  # ============================================================================
  postgres-audit-vault:
    container_name: postgres-audit-vault
    image: postgres:16-alpine
    restart: unless-stopped
    ports:
      - "5432:5432"
    environment:
      - POSTGRES_DB=banking_audit
      - POSTGRES_USER=audit_user
      - POSTGRES_PASSWORD=audit_password
    volumes:
      - ./postgres/init.sql:/docker-entrypoint-initdb.d/init.sql:ro
      - postgres_data:/var/lib/postgresql/data
    networks:
      - banking-net
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U audit_user -d banking_audit"]
      interval: 5s
      timeout: 5s
      retries: 10
      start_period: 5s

  # ============================================================================
  # 3. Distributed In-Memory Cache (Redis 7)
  # Container Name: redis-cache
  # Houses token blacklist, session families, and idempotency locks
  # ============================================================================
  redis-cache:
    container_name: redis-cache
    image: redis:7-alpine
    restart: unless-stopped
    ports:
      - "6379:6379"
    command: ["redis-server", "--appendonly", "yes"]
    networks:
      - banking-net

  # ============================================================================
  # 4. Database Web Console (Adminer with Oracle OCI8 & PostgreSQL)
  # Container Name: db-adminer
  # Web management GUI for database inspection and queries
  # ============================================================================
  adminer:
    container_name: db-adminer
    build:
      context: ./adminer
      dockerfile: Dockerfile
    pull_policy: build
    restart: unless-stopped
    ports:
      - "8088:8080"
    networks:
      - banking-net
    depends_on:
      - postgres-audit-vault
      - oracle-xe-master

  # ============================================================================
  # 5. Local Mock SMTP Server & Email Web Inbox (MailHog)
  # Container Name: mailhog-smtp
  # Catches all transaction receipt and maker-checker emails with live Web UI
  # ============================================================================
  mailhog:
    container_name: mailhog-smtp
    image: mailhog/mailhog:latest
    restart: unless-stopped
    ports:
      - "1025:1025"
      - "8025:8025"
    networks:
      - banking-net

  # ============================================================================
  # 6. Notification & Alert Service (Spring Boot :8083)
  # Container Name: notification-service
  # Asynchronous email receipt generation, Redis deduplication & teller alerts
  # ============================================================================
  notification-service:
    container_name: notification-service
    build:
      context: ../backend/notification-service
      dockerfile: Dockerfile
    restart: unless-stopped
    ports:
      - "8083:8083"
    environment:
      - SPRING_DATASOURCE_URL=jdbc:oracle:thin:@oracle-xe-master:1521/FREEPDB1
      - SPRING_DATASOURCE_USERNAME=SYSTEM
      - SPRING_DATASOURCE_PASSWORD=Password123#
      - SPRING_REDIS_HOST=redis-cache
      - SPRING_REDIS_PORT=6379
      - SPRING_MAIL_HOST=mailhog
      - SPRING_MAIL_PORT=1025
      - SPRING_KAFKA_BOOTSTRAP_SERVERS=localhost:9092
    networks:
      - banking-net
    depends_on:
      - oracle-xe-master
      - redis-cache
      - mailhog

volumes:
  oracle_data:
  postgres_data:

